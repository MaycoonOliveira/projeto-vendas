import type { Testimonial } from "@/types";

/**
 * Depoimentos individuais.
 *
 * VAZIO por padrão: NÃO inventamos citações nem nomes de hóspedes. A prova
 * social exibida no site usa apenas os dados AGREGADOS reais (nota 5,0 e o selo
 * "Favorito dos hóspedes" do Airbnb — ver siteConfig.reviews).
 *
 * TODO_CLIENTE: para exibir depoimentos, cole aqui citações reais e autorizadas
 * (idealmente com o primeiro nome do hóspede e o mês/ano). A seção passa a
 * mostrá-los automaticamente quando este array tiver itens.
 */
export const testimonials: Testimonial[] = [];
